[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "locker-finder"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.28",
    "flask>=3.1"
]